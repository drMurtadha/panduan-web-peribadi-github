import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://drmurtadha.github.io',
  base: '/panduan-web-peribadi-github',
  build: {
    inlineStylesheets: 'always',
  },
});
